select distinct ?resource ?comment ?classifier
where {
	?resource dcterms:description ?comment;
	DUL:classifies ?classifier
   	FILTER (regex(lcase(str(?comment)), "(bug|hack|later|todo|later2)","i")).
}
