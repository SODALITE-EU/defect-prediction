#PBS -S /bin/bash
#PBS -m abe
#PBS -N {{ job_name }}
#PBS -M {{ job_email }}
#PBS -l nodes={{ job_nb_nodes }}:ppn={{ job_ppn }},walltime={{ job_walltime }},gpus={{ job_gpus }}

/usr/local/bin/mpiexec -n $(( {{ job_nb_nodes }} * {{ job_ppn }} )) -machinefile $PBS_NODEFILE /usr/local/bin/singularity run prob-mapping/